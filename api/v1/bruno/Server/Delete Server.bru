meta {
  name: Delete Server
  type: http
  seq: 7
}

delete {
  url: {{url}}:{{port}}/api/v1/server/destroy/{{game_uid}}.{{server_name}}
  body: none
  auth: inherit
}

headers {
  X-Api-Key: {{api-key}}
}

settings {
  encodeUrl: true
}
