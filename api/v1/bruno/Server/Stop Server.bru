meta {
  name: Stop Server
  type: http
  seq: 4
}

put {
  url: {{url}}:{{port}}/api/v1/server/stop/{{game_uid}}.{{server_name}}
  body: none
  auth: inherit
}

headers {
  X-Api-Key: {{api-key}}
}

settings {
  encodeUrl: true
}
